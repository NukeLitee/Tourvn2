# Thêm link kết nối MongoDB Atlas của bạn vào đây
MONGO_URI=mongodb+srv://haodeng17_tourvn_db_user:18082005@cluster0.h3vxcu6.mongodb.net/?appName=Cluster0
PORT=5000

# Thêm một chuỗi bí mật bất kỳ cho JSON Web Token
JWT_SECRET=yourverysecretkey12345



http://googleusercontent.com/immersive_entry_chip/2


http://googleusercontent.com/immersive_entry_chip/3


http://googleusercontent.com/immersive_entry_chip/4


http://googleusercontent.com/immersive_entry_chip/5


http://googleusercontent.com/immersive_entry_chip/6


http://googleusercontent.com/immersive_entry_chip/7


http://googleusercontent.com/immersive_entry_chip/8


http://googleusercontent.com/immersive_entry_chip/90123

### Cách chạy Backend

1.  **Cài đặt:** Mở terminal, đi tới thư mục `backend` và chạy:
    ```bash
    npm install
    2.  **Cấu hình:** Mở file `.env` và thêm chuỗi kết nối MongoDB Atlas của bạn.
3.  **Chạy:**
    ```bash
    npm run dev
        Server sẽ chạy trên `http://localhost:5000`.

### Bước tiếp theo (Frontend)

Bây giờ backend đã sẵn sàng. Bước tiếp theo là bạn cần cập nhật code Frontend để:
1.  **Gọi API đăng nhập/đăng ký** trong `LoginPage` (thay vì submit form tĩnh) và lưu token vào `localStorage`.
2.  **Gọi API (`/api/tours`)** trong `TourCardDiv`, `SectionChotKeo`... (thay vì dùng `data.js`) để lấy dữ liệu tour động.
3.  **Gọi API (`/api/bookings`)** trong `CartPage` khi nhấn "Thanh toán", gửi `cartItems` và `token` (trong header) lên server.